version: "3.9"

services:
  bot:
    build: .
    container_name: swag-vpn-bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./.env:/app/.env
    environment:
      - TZ=Europe/Moscow
